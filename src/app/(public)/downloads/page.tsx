import PageHero from '@/components/PageHero';import {active} from '@/lib/queries';export const dynamic='force-dynamic';export default async function Page(){const items=await active('downloads');return <main><PageHero title="Downloads" text="Access company profiles, brochures, catalogues and useful documents."/><section className="section"><div className="container grid grid-3">{items.length?items.map((x:any)=><a className="admin-card" key={x.id} href={x.file_url}><div className="eyebrow">{x.category}</div><h3>{x.title}</h3><p>{x.description}</p><span className="btn btn-red">Download</span></a>):<div className="notice">Documents will be published here through the administration dashboard.</div>}</div></section></main>}
