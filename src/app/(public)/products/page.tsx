import PageHero from '@/components/PageHero';import {active} from '@/lib/queries';import ProductCards from '@/components/ProductCards';
export const dynamic='force-dynamic';export default async function Page(){const items=await active('products');return <main><PageHero title="Products" text="Discover systems designed for residential, commercial and institutional spaces."/><section className="section"><div className="container">{items.length?<ProductCards items={items}/>:<div className="notice">Content will appear here after it is added through the administration dashboard.</div>}</div></section></main>}
