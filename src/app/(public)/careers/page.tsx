import PageHero from '@/components/PageHero';import {active} from '@/lib/queries';export const dynamic='force-dynamic';export default async function Page(){const jobs=await active('vacancies');return <main><PageHero title="Careers" text="Build your career with a company committed to technical excellence and quality workmanship."/><section className="section"><div className="container grid">{jobs.length?jobs.map((j:any)=><div className="admin-card" key={j.id}><div className="eyebrow">{j.location} · {j.employment_type}</div><h2>{j.title}</h2><p>{j.description}</p><p><strong>Requirements:</strong> {j.requirements}</p><p><strong>Deadline:</strong> {j.deadline}</p></div>):<div className="notice">There are no open vacancies at present. Please check again later.</div>}</div></section></main>}
