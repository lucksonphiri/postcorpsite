import PageHero from '@/components/PageHero';import {active} from '@/lib/queries';import ProjectFilter from '@/components/ProjectFilter';
export const dynamic='force-dynamic';export default async function Page(){const items=await active('projects');return <main><PageHero title="Projects" text="Filter completed installations and construction partnerships by project category."/><section className="section"><div className="container">{items.length?<ProjectFilter items={items}/>:<div className="notice">Content will appear here after it is added through the administration dashboard.</div>}</div></section></main>}
