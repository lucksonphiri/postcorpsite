import PageHero from '@/components/PageHero';import {ContentCards} from '@/components/Cards';import {active} from '@/lib/queries';
export const dynamic='force-dynamic';export default async function Page(){const items=await active('projects');return <main><PageHero title="Projects" text="Selected installations and construction partnerships across Zimbabwe."/><section className="section"><div className="container">{items.length?<ContentCards items={items}/>:<div className="notice">Content will appear here after it is added through the administration dashboard.</div>}</div></section></main>}
