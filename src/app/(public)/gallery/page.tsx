import PageHero from '@/components/PageHero';import {active} from '@/lib/queries';import LightboxGallery from '@/components/LightboxGallery';
export const dynamic='force-dynamic';export default async function Page(){const items=await active('gallery_items');return <main><PageHero title="Project Gallery" text="Click any image to view it in a full-screen lightbox."/><section className="section"><div className="container">{items.length?<LightboxGallery items={items}/>:<div className="notice">Content will appear here after it is added through the administration dashboard.</div>}</div></section></main>}
