import {NextResponse} from 'next/server';import {sql} from '@/lib/db';export async function POST(req:Request){try{const b=await req.json();await sql`INSERT INTO quote_requests(name,company,phone,email,location,service,message) VALUES(${b.name},${b.company||null},${b.phone},${b.email||null},${b.location||null},${b.service||null},${b.message})`;return NextResponse.json({ok:true},{status:201})}catch{return NextResponse.json({error:'Unable to submit request'},{status:500})}}
