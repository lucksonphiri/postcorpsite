import {NextResponse} from 'next/server';import {sql} from '@/lib/db';export async function POST(req:Request){try{const b=await req.json();await sql`INSERT INTO enquiries(name,email,phone,subject,message) VALUES(${b.name},${b.email||null},${b.phone},${b.subject||null},${b.message})`;return NextResponse.json({ok:true},{status:201})}catch{return NextResponse.json({error:'Unable to submit enquiry'},{status:500})}}
