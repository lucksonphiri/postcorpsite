'use client';import {useState} from 'react';import {useRouter} from 'next/navigation';export default function LoginForm(){const[e,setE]=useState('');const r=useRouter();async function go(x:any){x.preventDefault();const b=Object.fromEntries(new FormData(x.currentTarget));const res=await fetch('/api/auth/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(b)});if(res.ok){r.refresh();location.href='/admin'}else setE('Incorrect email or password.')}return <form className="form" onSubmit={go}>{e&&<div className="notice">{e}</div>}<input className="input" name="email" type="email" required placeholder="Email address"/><input className="input" name="password" type="password" required placeholder="Password"/><button className="btn btn-red">Log In</button></form>}
