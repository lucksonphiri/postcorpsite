"use client";
import {useState} from "react";
export default function Chatbot(){const[open,setOpen]=useState(false);const[text,setText]=useState("");const[messages,setMessages]=useState([{who:"bot",text:"Welcome to Postcorp. How may I help you with glass, aluminium or shopfitting services?"}]);const send=async(e:React.FormEvent)=>{e.preventDefault();if(!text.trim())return;const q=text;setText("");setMessages(m=>[...m,{who:"user",text:q}]);try{const r=await fetch("/api/chatbot",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({message:q})});const d=await r.json();setMessages(m=>[...m,{who:"bot",text:d.answer}]);}catch{setMessages(m=>[...m,{who:"bot",text:"Please contact our team on +263 77 295 7823 for immediate assistance."}]);}};return <>{open&&<div className="chatbox"><div className="chat-head"><strong>Postcorp Assistant</strong><div style={{fontSize:12,color:'#bbb'}}>Product guidance · Quotes · Branches</div></div><div className="chat-body">{messages.map((m,i)=><div key={i} className={`msg ${m.who}`}>{m.text}</div>)}</div><form className="chat-form" onSubmit={send}><input value={text} onChange={e=>setText(e.target.value)} placeholder="Type your question..."/><button className="btn btn-red">Send</button></form></div>}<button className="chat-launch" onClick={()=>setOpen(!open)} aria-label="Open chatbot">{open?'×':'✦'}</button></>}
