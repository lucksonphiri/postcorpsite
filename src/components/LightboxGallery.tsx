'use client';import {useState} from 'react';
export default function LightboxGallery({items}:{items:any[]}){const[selected,setSelected]=useState<any>(null);return <><div className="gallery-grid">{items.map(x=><button className="gallery-tile" key={x.id} onClick={()=>setSelected(x)}><img src={x.image_url} alt={x.title}/><span>{x.title}</span></button>)}</div>{selected&&<div className="lightbox" onClick={()=>setSelected(null)}><button aria-label="Close">✕</button><img src={selected.image_url} alt={selected.title}/><div><h3>{selected.title}</h3><p>{selected.description}</p></div></div>}</>}
