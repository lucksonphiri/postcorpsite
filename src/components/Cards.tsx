export function ContentCards({items}:{items:any[]}){return <div className="grid grid-3">{items.map((x:any)=><article className="card" key={x.id}>{x.image_url&&<img src={x.image_url} alt={x.title||x.name}/>}<div className="card-body"><div className="eyebrow" style={{fontSize:11}}>{x.category||x.location||"Postcorp"}</div><h3>{x.title||x.name}</h3><p>{x.summary||x.excerpt||x.description}</p></div></article>)}</div>}
