'use client';import {useEffect,useState} from 'react';
export default function UserManagement(){const[users,setUsers]=useState<any[]>([]),[msg,setMsg]=useState('');const load=()=>fetch('/api/users').then(r=>r.json()).then(setUsers);useEffect(()=>{load()},[]);async function save(e:any){e.preventDefault();const body=Object.fromEntries(new FormData(e.currentTarget));const r=await fetch('/api/users',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)});setMsg(r.ok?'Content user created successfully':'Unable to create user');if(r.ok){e.currentTarget.reset();load()}}return <div><div className="eyebrow">Security</div><h1>Users</h1>{msg&&<div className="notice">{msg}</div>}<div className="cms-layout"><form className="admin-card form" onSubmit={save}><h3>Create Content Editor</h3><input className="input" name="name" required placeholder="Full name"/><input className="input" name="email" type="email" required placeholder="Email"/><input className="input" name="password" type="password" minLength={8} required placeholder="Temporary password"/><select name="role"><option value="content_editor">Content Editor</option><option value="administrator">Administrator</option></select><button className="btn btn-red">Create User</button></form><div className="admin-card table-wrap"><table><thead><tr><th>Name</th><th>Email</th><th>Role</th><th>Status</th></tr></thead><tbody>{Array.isArray(users)&&users.map(u=><tr key={u.id}><td>{u.name}</td><td>{u.email}</td><td>{u.role}</td><td>{u.is_active?'Active':'Disabled'}</td></tr>)}</tbody></table></div></div></div>}
